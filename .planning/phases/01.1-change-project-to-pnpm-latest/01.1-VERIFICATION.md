---
phase: 01.1-change-project-to-pnpm-latest
verified: 2026-03-27T00:00:00Z
status: passed
score: 8/8 must-haves verified
re_verification: false
---

# Phase 01.1: Change Project to pnpm Verification Report

**Phase Goal:** Migrate the repository from npm to pnpm@10.33.0 so all local tooling, git hooks, and CI use a single pinned package manager without introducing dependency drift, hoisting config, or unrelated changes.
**Verified:** 2026-03-27
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `package.json` pins `pnpm@10.33.0` as the repo package manager | VERIFIED | `node -e` confirms `packageManager` field is exactly `pnpm@10.33.0` |
| 2 | `pnpm-lock.yaml` exists and `package-lock.json` is removed | VERIFIED | `pnpm-lock.yaml` present at repo root; `package-lock.json` absent |
| 3 | No `.npmrc`, `pnpm-workspace.yaml`, hoisting config introduced | VERIFIED | Both files absent; no `shamefully-hoist`/`publicHoistPattern` in `package.json` or `pnpm-lock.yaml` |
| 4 | Sentinel versions `@adonisjs/core@7.1.1`, `@adonisjs/assembler@8.2.0`, `lefthook@2.1.4` preserved in `pnpm-lock.yaml` | VERIFIED | Grep counts: core 27, assembler 50, lefthook 2 — all present |
| 5 | Makefile runs quality tools via `pnpm exec` and has an `audit` target | VERIFIED | `make -n lint/lint-fix/typecheck/audit` all print correct `pnpm exec` / `pnpm audit` recipes; no `npx` remains |
| 6 | `lefthook.yml` uses `pnpm exec` for all hook commands; no `npx` remains | VERIFIED | All four hook commands (`eslint`, `prettier`, `tsc`, `commitlint`) use `pnpm exec`; no `npx` found |
| 7 | GitHub Actions installs pnpm via `pnpm/action-setup@v5` and Node via `actions/setup-node@v6` with `cache: 'pnpm'` | VERIFIED | Exactly 4 occurrences each of `pnpm/action-setup@v5`, `actions/setup-node@v6`, `cache: 'pnpm'` in `ci.yml` |
| 8 | CI install steps use `pnpm install --frozen-lockfile`; no `npm ci`/`npm audit` remain; security job uses `make audit` | VERIFIED | 4 `pnpm install --frozen-lockfile` entries; 1 `run: make audit`; zero `npm ci` / `npm audit` |

**Score:** 8/8 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `package.json` | Contains `"packageManager": "pnpm@10.33.0"` | VERIFIED | Exact string confirmed via `node -e` |
| `pnpm-lock.yaml` | Present; lockfileVersion 9.0; contains sentinel packages | VERIFIED | Exists, `lockfileVersion: '9.0'`, sentinels present |
| `package-lock.json` | Must be absent | VERIFIED | File not present |
| `.npmrc` | Must be absent | VERIFIED | File not present |
| `pnpm-workspace.yaml` | Must be absent | VERIFIED | File not present |
| `Makefile` | Contains `audit:` target using `pnpm audit --audit-level=high`; `pnpm exec` for lint/typecheck | VERIFIED | `audit:` at line 44; `pnpm audit --audit-level=high` confirmed |
| `lefthook.yml` | All commands use `pnpm exec`; no `npx` | VERIFIED | Lines 10, 13, 15, 20 confirmed |
| `.github/workflows/ci.yml` | `pnpm/action-setup@v5`, `actions/setup-node@v6`, `cache: 'pnpm'`, frozen install, `make audit` | VERIFIED | All counts exact; no npm remnants |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `package.json` | `pnpm-lock.yaml` | `packageManager` pin + frozen install | VERIFIED | Pin present; lockfile generated and valid |
| `package-lock.json` (removed) | `pnpm-lock.yaml` | `pnpm import` preserved resolution graph | VERIFIED | Sentinel versions match prior npm graph; `package-lock.json` removed after import |
| `Makefile` | `package.json` | `pnpm exec` resolves devDependencies | VERIFIED | All quality targets use `pnpm exec` |
| `lefthook.yml` | `package.json` | Hook commands use `pnpm exec` | VERIFIED | Four commands migrated; no `npx` |
| `.github/workflows/ci.yml` | `package.json` | `pnpm/action-setup@v5` reads `packageManager` pin | VERIFIED | 4 occurrences of `pnpm/action-setup@v5` |
| `.github/workflows/ci.yml` | `Makefile` | CI runs `make lint`, `make typecheck`, `make test`, `make build`, `make audit` | VERIFIED | All 5 targets present at lines 33, 36, 119, 140, 160 |

---

### Data-Flow Trace (Level 4)

Not applicable — this phase has no components that render dynamic data. All artifacts are config, build, and CI files.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `package.json` pins exact pnpm version | `node -e "process.stdout.write(JSON.parse(...).packageManager)"` | `pnpm@10.33.0` | PASS |
| `pnpm-lock.yaml` lockfile version is pnpm v9 format | `head -1 pnpm-lock.yaml` | `lockfileVersion: '9.0'` | PASS |
| `make -n lint` uses `pnpm exec` | `make -n lint` | `pnpm exec eslint . --max-warnings 0` | PASS |
| `make -n audit` uses `pnpm audit` | `make -n audit` | `pnpm audit --audit-level=high` | PASS |
| No `npx` in `Makefile` | `grep npx Makefile` | no output | PASS |
| All `lefthook.yml` hooks use `pnpm exec` | `grep -n "npx\|pnpm exec" lefthook.yml` | 4 `pnpm exec` lines, 0 `npx` | PASS |
| CI has exactly 4 `pnpm/action-setup@v5` | count grep | 4 | PASS |
| CI has exactly 4 `pnpm install --frozen-lockfile` | count grep | 4 | PASS |
| CI has zero `npm ci` / `npm audit` | grep | no output | PASS |

---

### Requirements Coverage

No requirement IDs were explicitly mapped for this inserted phase. Not applicable.

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| None | — | — | — |

No TODO/FIXME markers, placeholder comments, empty stubs, or npm-era invocations detected in any modified file.

---

### Human Verification Required

None. All phase goals are fully verifiable through static code and config inspection.

---

### Gaps Summary

No gaps. All 8 observable truths are verified:

- `package.json` carries the exact `pnpm@10.33.0` pin.
- `pnpm-lock.yaml` exists with lockfileVersion 9.0 and all three sentinel package versions intact.
- `package-lock.json`, `.npmrc`, and `pnpm-workspace.yaml` are all absent; no hoisting config was introduced.
- `Makefile` quality targets use `pnpm exec`; the `audit` target is present with the correct recipe.
- `lefthook.yml` uses `pnpm exec` for all four hook commands with no `npx` remaining.
- `.github/workflows/ci.yml` has exactly 4 occurrences each of `pnpm/action-setup@v5`, `actions/setup-node@v6`, `cache: 'pnpm'`, and `pnpm install --frozen-lockfile`; `make audit` is the security job command; no `npm ci` or `npm audit` remain.

The migration is complete and consistent across local tooling, git hooks, and CI.

---

_Verified: 2026-03-27_
_Verifier: Claude (gsd-verifier)_
