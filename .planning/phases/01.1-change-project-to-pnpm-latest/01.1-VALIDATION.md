---
phase: "01.1"
slug: change-project-to-pnpm-latest
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-27
---

# Phase 01.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Japa (`@japa/runner` v5) + AdonisJS build/typecheck smoke checks |
| **Config file** | `adonisrc.ts` (`japa.config.ts` is a documentation artifact) |
| **Quick run command** | `Run the active task's exact <automated> verify command from its PLAN.md entry` |
| **Full suite command** | `make typecheck && make build && make test` in a Node 24 environment; run `make lint` in a clean checkout or CI |
| **Estimated runtime** | ~5-30 seconds per task; ~90 seconds for the full Node 24 phase gate |

---

## Sampling Rate

- **After every task commit:** Run that task's exact `<automated>` verify command from the plan
- **After every plan wave:** Run `make typecheck && make build`
- **Before `$gsd-verify-work`:** Full suite must be green in a Node 24 environment
- **Max feedback latency:** 30 seconds for task-level feedback

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 01.1-01-01 | 01 | 1 | PM-01 | static | `test "$(node -e "process.stdout.write(JSON.parse(require('fs').readFileSync('package.json','utf8')).packageManager)")" = "pnpm@10.33.0" && test ! -f .npmrc && test ! -f pnpm-workspace.yaml && ! rg -n "shamefully-hoist|shamefullyHoist|publicHoistPattern" package.json 2>/dev/null` | ✅ Existing | ⬜ pending |
| 01.1-01-02 | 01 | 1 | PM-05 | smoke | `test -f pnpm-lock.yaml && test ! -f package-lock.json && test ! -f .npmrc && test ! -f pnpm-workspace.yaml && ! rg -n "shamefully-hoist|shamefullyHoist|publicHoistPattern" package.json pnpm-lock.yaml 2>/dev/null && rg -n "@adonisjs/core@7\.1\.1" pnpm-lock.yaml && rg -n "@adonisjs/assembler@8\.2\.0" pnpm-lock.yaml && rg -n "lefthook@2\.1\.4" pnpm-lock.yaml && pnpm install --frozen-lockfile` | ❌ W0 | ⬜ pending |
| 01.1-02-01 | 02 | 2 | PM-02 | static | `make -n lint | rg "pnpm exec eslint \. --max-warnings 0" && make -n lint-fix | rg "pnpm exec eslint \. --fix --max-warnings 0" && make -n typecheck | rg "pnpm exec tsc --noEmit" && make -n audit | rg "pnpm audit --audit-level=high" && ! rg -n "\bnpx\b" Makefile` | ✅ Existing | ⬜ pending |
| 01.1-02-02 | 02 | 2 | PM-02 | static | `! rg -n "\bnpx\b" lefthook.yml && rg -n "pnpm exec eslint --max-warnings 0 \{staged_files\}" lefthook.yml && rg -n "pnpm exec prettier --write \{staged_files\} && git add \{staged_files\}" lefthook.yml && rg -n "pnpm exec tsc --noEmit" lefthook.yml && rg -n "pnpm exec commitlint --edit \{1\}" lefthook.yml` | ✅ Existing | ⬜ pending |
| 01.1-03-01 | 03 | 3 | PM-04 | static | `test "$(rg -o "pnpm/action-setup@v5" .github/workflows/ci.yml | wc -l)" -eq 4 && test "$(rg -o "actions/setup-node@v6" .github/workflows/ci.yml | wc -l)" -eq 4 && test "$(rg -o "cache: 'pnpm'" .github/workflows/ci.yml | wc -l)" -eq 4 && ! rg -n "actions/setup-node@v4|cache: 'npm'" .github/workflows/ci.yml` | ✅ Existing | ⬜ pending |
| 01.1-03-02 | 03 | 3 | PM-03 | static | `test "$(rg -o "pnpm install --frozen-lockfile" .github/workflows/ci.yml | wc -l)" -eq 4 && test "$(rg -o "run: make lint" .github/workflows/ci.yml | wc -l)" -eq 1 && test "$(rg -o "run: make typecheck" .github/workflows/ci.yml | wc -l)" -eq 1 && test "$(rg -o "run: make test" .github/workflows/ci.yml | wc -l)" -eq 1 && test "$(rg -o "run: make build" .github/workflows/ci.yml | wc -l)" -eq 1 && test "$(rg -o "run: make audit" .github/workflows/ci.yml | wc -l)" -eq 1 && ! rg -n "npm ci|npm audit" .github/workflows/ci.yml` | ✅ Existing | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. No new test stubs, fixtures, framework installs, or helper scaffolds are required for this tooling-only phase.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Lefthook executes through pnpm-managed binaries on commit | PM-02 | Requires a real git hook invocation | Stage a small TypeScript change, run `git commit`, and verify pre-commit runs `pnpm exec eslint`, `pnpm exec prettier`, and `pnpm exec tsc` |
| GitHub Actions jobs install/cache/audit with pnpm | PM-04 | Requires hosted GitHub Actions environment | Push branch or open PR, then confirm all CI jobs show pnpm setup, pnpm cache restore, frozen-lockfile install, and audit step execution |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 30s for task-level feedback
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-03-27
