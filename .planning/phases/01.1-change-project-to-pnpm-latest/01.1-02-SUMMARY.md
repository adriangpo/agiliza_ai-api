---
phase: 01.1-change-project-to-pnpm-latest
plan: 02
subsystem: infra
tags: [pnpm, makefile, lefthook, eslint, prettier, tsc, commitlint, git-hooks]

requires:
  - phase: 01.1-change-project-to-pnpm-latest-01
    provides: pnpm lockfile migration (package-lock.json removed, pnpm-lock.yaml created, packageManager field set)

provides:
  - Makefile quality targets (lint, lint-fix, typecheck) use pnpm exec instead of npx
  - Makefile exposes audit target using pnpm audit --audit-level=high
  - lefthook git hooks execute repo-local binaries via pnpm exec with no npx invocations

affects: [CI, contributors, git-hooks, quality-tooling]

tech-stack:
  added: []
  patterns:
    - "All repo CLI tool execution goes through pnpm exec (not npx) — tools resolve from local node_modules"
    - "make audit is the standard security check entry point for local and CI use"

key-files:
  created: []
  modified:
    - Makefile
    - lefthook.yml

key-decisions:
  - "pnpm exec chosen over pnpm run for tool invocations — exec resolves binaries from node_modules/.bin without requiring a package.json script entry"
  - "audit target uses pnpm audit --audit-level=high — high threshold matches CI security gate convention"

patterns-established:
  - "Quality commands: pnpm exec <tool> is the standard invocation form across Makefile and lefthook"
  - "make audit: standard entry point for security audits, mirrors pnpm audit --audit-level=high"

requirements-completed: []

duration: 1min
completed: 2026-03-27
---

# Phase 01.1 Plan 02: Migrate Command Surface to pnpm exec Summary

**Makefile quality targets (lint, lint-fix, typecheck) and lefthook git hooks migrated from npx to pnpm exec, plus a new `make audit` target using `pnpm audit --audit-level=high`**

## Performance

- **Duration:** ~1 min
- **Started:** 2026-03-27T18:59:02Z
- **Completed:** 2026-03-27T18:59:56Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Replaced all `npx` invocations in `Makefile` (lint, lint-fix, typecheck) with `pnpm exec` equivalents
- Added `audit:` target to `Makefile` with recipe `pnpm audit --audit-level=high` and listed it in `.PHONY`
- Replaced all `npx` invocations in `lefthook.yml` (eslint, prettier, tsc, commitlint) with `pnpm exec` equivalents
- Confirmed via `make -n` dry-runs that all targets produce the expected pnpm commands
- Confirmed zero remaining `npx` invocations in both files

## Task Commits

Each task was committed atomically:

1. **Task 1: Update Makefile quality targets to use pnpm exec and add make audit** - `f272f97` (chore)
2. **Task 2: Update lefthook commands to use pnpm exec for repo-local tools** - `f808116` (chore)

## Files Created/Modified

- `Makefile` - lint, lint-fix, and typecheck targets use pnpm exec; audit target added
- `lefthook.yml` - all hook commands (eslint, prettier, tsc, commitlint) use pnpm exec

## Decisions Made

- `pnpm exec` chosen over `pnpm run` because it resolves binaries directly from `node_modules/.bin` without requiring a corresponding script entry in `package.json` — keeps Makefile and hook commands independent of the scripts section
- `--audit-level=high` chosen for the audit target to fail only on high/critical severity vulnerabilities, matching the expected CI security gate threshold

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 01.1 plan 02 complete: Makefile and lefthook are fully pnpm-native
- Plan 03 (if present) can proceed — the full pnpm command surface is consistent
- Contributors running `make lint`, `make typecheck`, or committing code will now use pnpm exec without any npx fallback

---
*Phase: 01.1-change-project-to-pnpm-latest*
*Completed: 2026-03-27*

## Self-Check: PASSED

- FOUND: Makefile
- FOUND: lefthook.yml
- FOUND: .planning/phases/01.1-change-project-to-pnpm-latest/01.1-02-SUMMARY.md
- FOUND commit f272f97 (Task 1: Makefile)
- FOUND commit f808116 (Task 2: lefthook.yml)
- FOUND commit 6cbf5d3 (Plan metadata)
