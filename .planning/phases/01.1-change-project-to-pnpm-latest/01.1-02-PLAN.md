---
phase: 01.1-change-project-to-pnpm-latest
plan: 02
type: execute
wave: 2
depends_on:
  - 01.1-01
files_modified:
  - Makefile
  - lefthook.yml
autonomous: true
requirements: []
must_haves:
  truths:
    - "Makefile runs repo-local quality tools through pnpm exec instead of npx"
    - "Makefile exposes an audit target for local and CI security checks"
    - "Lefthook runs repo-local binaries through pnpm exec and no npx invocations remain"
  artifacts:
    - path: "Makefile"
      provides: "pnpm-based lint, lint-fix, typecheck, and audit command surface"
      contains: "audit:"
    - path: "lefthook.yml"
      provides: "pnpm-based git hook commands"
      contains: "pnpm exec"
  key_links:
    - from: "Makefile"
      to: "package.json"
      via: "pnpm exec resolves tools from repo dependencies"
      pattern: "pnpm exec"
    - from: "lefthook.yml"
      to: "package.json"
      via: "hook commands execute declared devDependencies instead of npx"
      pattern: "pnpm exec"
---

<objective>
Migrate the human and git-hook command surface from `npx` to `pnpm exec`, and add a `make audit` target so later CI work can stay aligned with the repo’s Makefile-first convention.

Purpose: The lockfile migration is incomplete if contributors and hooks still invoke npm-era commands. This plan keeps the repo’s established “Makefile is law” pattern from Phase 1 while switching command execution to pnpm without touching unrelated build, test, or database targets.
Output: updated `Makefile` and `lefthook.yml` using `pnpm exec`, plus a new `audit` target.
</objective>

<execution_context>
@/home/adrian/.codex/get-shit-done/workflows/execute-plan.md
@/home/adrian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-change-project-to-pnpm-latest/01.1-RESEARCH.md
@Makefile
@lefthook.yml
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update Makefile quality targets to use pnpm exec and add make audit</name>
  <files>Makefile</files>
  <read_first>
    - Makefile
    - package.json
    - .planning/phases/01.1-change-project-to-pnpm-latest/01.1-RESEARCH.md
    - .planning/phases/01-foundation/01-03-SUMMARY.md
  </read_first>
  <action>
    Edit the existing `Makefile` in place. Keep the file’s comments, target names, Docker targets, `node ace` commands, database targets, and `setup-db` target intact.

    Apply only these concrete changes:
    - Add `audit` to the `.PHONY` list.
    - Replace `npx eslint . --max-warnings 0` with `pnpm exec eslint . --max-warnings 0`.
    - Replace `npx eslint . --fix --max-warnings 0` with `pnpm exec eslint . --fix --max-warnings 0`.
    - Replace `npx tsc --noEmit` with `pnpm exec tsc --noEmit`.
    - Add a new `audit:` target with the exact recipe `pnpm audit --audit-level=high`.

    Do not rewrite the whole file and do not change any `node ace ...` command in this plan.
  </action>
  <verify>
    <automated>make -n lint | rg "pnpm exec eslint \\. --max-warnings 0" && make -n lint-fix | rg "pnpm exec eslint \\. --fix --max-warnings 0" && make -n typecheck | rg "pnpm exec tsc --noEmit" && make -n audit | rg "pnpm audit --audit-level=high" && ! rg -n "\\bnpx\\b" Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `make -n lint` prints `pnpm exec eslint . --max-warnings 0`
    - `make -n lint-fix` prints `pnpm exec eslint . --fix --max-warnings 0`
    - `make -n typecheck` prints `pnpm exec tsc --noEmit`
    - `make -n audit` prints `pnpm audit --audit-level=high`
    - `Makefile` contains no remaining `npx` invocations
    - Existing non-quality targets remain present and unchanged in intent
  </acceptance_criteria>
  <done>The Makefile exposes pnpm-based lint, fix, typecheck, and audit targets without disturbing the existing build/test/database surface.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update lefthook commands to use pnpm exec for repo-local tools</name>
  <files>lefthook.yml</files>
  <read_first>
    - lefthook.yml
    - package.json
    - Makefile
    - .planning/phases/01.1-change-project-to-pnpm-latest/01.1-RESEARCH.md
    - .planning/phases/01-foundation/01-02-SUMMARY.md
  </read_first>
  <action>
    Edit `lefthook.yml` in place and preserve the existing hook names, `parallel: false`, and globs.

    Replace each hook command with the exact pnpm form below:
    - `pnpm exec eslint --max-warnings 0 {staged_files}`
    - `pnpm exec prettier --write {staged_files} && git add {staged_files}`
    - `pnpm exec tsc --noEmit`
    - `pnpm exec commitlint --edit {1}`

    Guardrails:
    - Do not switch to `pnpm dlx`.
    - Do not change the `prepare` script in `package.json`.
    - Do not alter which files each hook targets.
  </action>
  <verify>
    <automated>! rg -n "\\bnpx\\b" lefthook.yml && rg -n "pnpm exec eslint --max-warnings 0 \\{staged_files\\}" lefthook.yml && rg -n "pnpm exec prettier --write \\{staged_files\\} && git add \\{staged_files\\}" lefthook.yml && rg -n "pnpm exec tsc --noEmit" lefthook.yml && rg -n "pnpm exec commitlint --edit \\{1\\}" lefthook.yml</automated>
  </verify>
  <acceptance_criteria>
    - `lefthook.yml` contains no `npx` invocations
    - The pre-commit lint command is exactly `pnpm exec eslint --max-warnings 0 {staged_files}`
    - The pre-commit format command is exactly `pnpm exec prettier --write {staged_files} && git add {staged_files}`
    - The pre-commit typecheck command is exactly `pnpm exec tsc --noEmit`
    - The commit-msg command is exactly `pnpm exec commitlint --edit {1}`
  </acceptance_criteria>
  <done>Git hooks execute declared repo dependencies via pnpm instead of fetching binaries through npx.</done>
</task>

</tasks>

<verification>
Run `make -n lint`, `make -n lint-fix`, `make -n typecheck`, and `make -n audit` to confirm the Makefile recipes use pnpm.
Run `! rg -n "\\bnpx\\b" Makefile lefthook.yml` to confirm the repo command surface is free of npm-era tool execution.
</verification>

<success_criteria>
- `Makefile` uses `pnpm exec` for lint, lint-fix, and typecheck
- `Makefile` has an `audit` target using `pnpm audit --audit-level=high`
- `lefthook.yml` uses `pnpm exec` for eslint, prettier, tsc, and commitlint
- No `npx` command remains in `Makefile` or `lefthook.yml`
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-change-project-to-pnpm-latest/01.1-02-SUMMARY.md`
</output>
