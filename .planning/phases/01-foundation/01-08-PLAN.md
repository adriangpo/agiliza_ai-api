---
phase: 01-foundation
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: false
requirements: []
gap_closure: true

must_haves:
  truths:
    - "package.json declares engines >= 24 so npm warns when run on an incompatible Node.js version"
    - "Node.js 24 is active on the developer's machine"
  artifacts:
    - path: "package.json"
      provides: "engines constraint declaring Node.js >=24"
      contains: '"engines"'
  key_links:
    - from: "package.json"
      to: "Node.js runtime"
      via: '"engines": { "node": ">=24" }'
      pattern: '"engines"'
---

<objective>
Add the `engines` field to `package.json` to enforce the Node.js >=24 constraint programmatically, and provide a human checkpoint to install Node.js 24 on the developer's machine.

Purpose: CLAUDE.md and AdonisJS v7 both require Node.js 24. The machine runs v22.22.0 and there is currently nothing in the codebase to signal this constraint to tooling (npm, CI, other contributors).
Output: `package.json` with `engines` field; Node.js 24 active on the machine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add engines field to package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json — read the full file first; the `engines` field must be inserted at the top level, after the `"private": true` line and before the `"type": "module"` line (or anywhere at the top level — order does not matter to npm, but alphabetical after `"private"` is conventional)
  </read_first>
  <action>
    Open `package.json` and add the following top-level field:

    ```json
    "engines": {
      "node": ">=24"
    },
    ```

    Insert it after `"private": true` so the ordering reads: `name`, `version`, `private`, `engines`, `type`, ...

    Do NOT change any other field. Do NOT add `"npm"` to the engines object — only `"node"` is required.

    The exact string to add (including the comma on the closing brace since `"type"` follows):
    ```
    "engines": {
      "node": ">=24"
    },
    ```
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); if (!p.engines || p.engines.node !== '>=24') process.exit(1); console.log('engines field OK:', p.engines)"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"engines"` key at the top level
    - `package.json` contains `"node": ">=24"` inside the `engines` object
    - `package.json` is valid JSON (no syntax errors — node -e parse above must not throw)
    - No other fields in `package.json` are changed
  </acceptance_criteria>
  <done>package.json contains `"engines": { "node": ">=24" }` and is valid JSON</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Install Node.js 24 on developer machine</name>
  <files>none — machine-level action, no codebase files modified</files>
  <what-built>
    The codebase now declares `engines: { node: ">=24" }`. The machine still runs Node.js v22.22.0, which violates the project constraint.
  </what-built>
  <action>
    Human action required: install Node.js 24 using your version manager and switch the active version to v24.x.x. This cannot be automated by Claude — it requires system-level package installation.

    **Option A — fnm (recommended):**
    ```bash
    fnm install 24
    fnm use 24
    node --version   # must print v24.x.x
    ```

    **Option B — nvm:**
    ```bash
    nvm install 24
    nvm use 24
    node --version   # must print v24.x.x
    ```

    **Option C — system package manager (Fedora/dnf):**
    ```bash
    sudo dnf module enable nodejs:24
    sudo dnf install nodejs
    node --version   # must print v24.x.x
    ```

    After installation, verify the project still boots:
    ```bash
    node --version   # v24.x.x
    node ace --help  # AdonisJS CLI responds without errors
    ```

    Optionally add a `.nvmrc` or `.node-version` file in the project root containing `24` to pin the version for future shells:
    ```bash
    echo "24" > .nvmrc
    ```
  </action>
  <how-to-verify>
    Run `node --version` — it must return `v24.x.x`.
    Run `node ace --help` — AdonisJS CLI must respond without errors.
  </how-to-verify>
  <verify>
    <automated>MISSING — machine-level Node.js installation cannot be verified by an automated codebase command. Human must confirm node --version returns v24.x.x.</automated>
  </verify>
  <done>node --version returns v24.x.x and node ace --help responds without errors</done>
  <resume-signal>Type "approved" once `node --version` returns v24.x.x, or describe what version manager you used</resume-signal>
</task>

</tasks>

<verification>
After both tasks:
- `package.json` contains `"engines": { "node": ">=24" }` (verifiable with grep or JSON parse)
- `node --version` returns `v24.x.x` on the developer machine
- `node ace --help` responds without errors under Node.js 24
</verification>

<success_criteria>
- package.json has the engines constraint so npm warns on incompatible Node versions
- Developer machine runs Node.js 24 as the active version
- No regression: existing scripts (start, build, dev, test, lint) remain unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-08-SUMMARY.md` following the standard summary template.
</output>
